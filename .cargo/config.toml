# Project runners (mirrors snert's one-command style).
# `cargo nested` runs 0xide in a nested Wayland window — our fast dev loop.
# (More runners — headless, tty, vm — will arrive as those stages land.)
[alias]
nested = "run"
